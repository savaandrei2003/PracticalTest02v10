<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Numele pokemon -->
    <EditText
        android:id="@+id/pokemon_name_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Pokemon name (e.g. pikachu)" />

    <!-- Buton cerere -->
    <Button
        android:id="@+id/get_info_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Fetch Pokemon" />

    <!-- (Al doilea buton) pornește serverul local -->
    <EditText
        android:id="@+id/server_port_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Server port (e.g. 5000)"
        android:inputType="number" />

    <Button
        android:id="@+id/start_server_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Start Server" />

    <!-- TextView pentru abilități -->
    <TextView
        android:id="@+id/abilities_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Abilities:"
        android:textSize="16sp"
        android:layout_marginTop="10dp" />

    <TextView
        android:id="@+id/types_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Types:"
        android:textSize="16sp"
        android:layout_marginTop="10dp" />

    <!-- ImageView pentru pokemon (din sprites/front_default) -->
    <ImageView
        android:id="@+id/pokemon_image_view"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginTop="10dp"
        android:contentDescription="Pokemon image" />

</LinearLayout>
